// Copy to secrets.h (gitignored) or run ./gen_secrets.sh to build it from
// sim/esphome/secrets.yaml — the sign bridge uses the same Wi-Fi as the room
// nodes. SERVER_HOST is where the storm button POSTs /api/sign_storm.
#pragma once

#define WIFI_SSID "lohp-maze"
#define WIFI_PASSWORD "change-me"
#define SERVER_HOST "192.168.253.185"  // bench: this dev box; playa: the server Pi
#define SERVER_PORT 5000
#define OTA_PASSWORD "change-me-too"
