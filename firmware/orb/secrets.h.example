// Copy to secrets.h (gitignored) or run ./gen_secrets.sh to build it from
// sim/esphome/secrets.yaml — the orb uses the same Wi-Fi as the room nodes.
#pragma once

#define WIFI_SSID "LOHP-ESP"       // the maze LAN AP (wiring-guides/maze-network.md)
#define WIFI_PASSWORD "change-me"
#define SERVER_HOST "192.168.252.231" // the server Pi (RUT DHCP reservation); bench: the dev box's LOHP-ESP address
#define SERVER_PORT 5000
#define OTA_PASSWORD "change-me-too"
