.aider*
__pycache__/
*.py[cod]
effects_config.json
client/cache/
photos/
data/
.env
*.log
.playwright-mcp/
.playwright-cli/
Background-images/
# generated streamable cue WAVs (make_node_audio.py)
audio_files/cues/
# generated crossfaded ambience loop cache (audio_manager.py)
audio_files/generated/
# audio console's one-deep undo copy (tools/audio_console.py) + its pidfile
audio_config.json.bak
audio_config.json.tmp
tools/audio-console.pid
# orb firmware credentials + build output
firmware/orb/secrets.h
firmware/orb/build/
firmware/orb/tools/preview_face

# sign-bridge firmware credentials + build output
firmware/sign/secrets.h
firmware/sign/build/
# maze network credentials cut-sheet sidecar (the repo is public)
wiring-guides/maze-network-credentials.md
.floor_theme
# AI video-base experiment: generated media stays local (~1 GB, regenerable
# from the tracked scripts + prompts + job logs — see its README). Tracked:
# scripts, prompts, job logs, skin npz packs, eyeball sheets, loop-seam
# anchors, and the browser-served base_loop_<theme>.mp4 assets.
experiments/video-base/*.npy
experiments/video-base/clip_*.mp4
experiments/video-base/*_archive*
experiments/video-base/*_preview.mp4
experiments/video-base/eval_*.mp4
experiments/video-base/bare_*.mp4
experiments/video-base/*.png
!experiments/video-base/*_sheet.png
!experiments/video-base/anchor_*.png
