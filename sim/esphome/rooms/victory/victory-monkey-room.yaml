# GENERATED by make_victory_bank.py — do not hand-edit.
# Monkey Room: victory cues baked into flash so the win lands the instant it is
# earned (Tim 2026-08-31; generalizes the Photo Bomb snap exception).
# The server calls the cue_<id> api action when the box carries the cue and
# streams the WAV as before when it doesn't. Source pools: MonkeyBusiness.

media_player:
  - id: !extend room_audio
    files:
      - id: vic_monkey_shrine_complete
        file: ../../../audio_files/cues/monkey_shrine_complete.wav

api:
  actions:
    - action: cue_monkey_shrine_complete
      then:
        - media_player.speaker.play_on_device_media_file:
            id: room_audio
            media_file: vic_monkey_shrine_complete
            announcement: true
