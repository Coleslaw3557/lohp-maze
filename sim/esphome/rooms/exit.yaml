# Exit sensor node — trigger map from client/config-unit-*.json
# No trigger packages yet: the Lightning-on-entry placeholder was test wiring
# (removed 2026-07-17). Include tripwire.yaml + an `effect` substitution once
# this room gets its bespoke effect.
substitutions:
  node_name: lohp-node-exit
  room: "Exit"
  server_host: "127.0.0.1"   # sim: local server; real node: 192.168.1.238
  api_port: "6075"
  host_mac: "06:35:69:00:00:0f"

packages:
  logic: !include ../packages/logic.yaml
  platform: !include ../packages/sim_host.yaml   # -> hardware_s3.yaml when flashing (S3 = fleet standard)
