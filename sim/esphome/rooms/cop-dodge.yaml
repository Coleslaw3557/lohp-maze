# Cop Dodge sensor node — trigger map from client/config-unit-*.json
substitutions:
  node_name: lohp-node-cop-dodge
  room: "Cop Dodge"
  effect: "PoliceLights"
  server_host: "127.0.0.1"   # sim: local server; real node: 192.168.1.238
  api_port: "6062"
  host_mac: "06:35:69:00:00:02"

packages:
  logic: !include ../packages/logic.yaml
  platform: !include ../packages/sim_host.yaml   # -> hardware_c3.yaml when flashing
