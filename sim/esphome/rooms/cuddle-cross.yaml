# Cuddle Cross sensor node — trigger map from triggers.json
substitutions:
  node_name: lohp-node-cuddle-cross
  room: "Cuddle Cross"
  effect: "CuddlePuddle"
  server_host: "127.0.0.1"   # sim: local server; real node: 192.168.252.231
  api_port: "6067"
  host_mac: "06:35:69:00:00:07"

packages:
  logic: !include ../packages/logic.yaml
  tripwire: !include ../packages/tripwire.yaml
  platform: !include ../packages/sim_host.yaml   # -> hardware_s3.yaml when flashing (S3 = fleet standard)

# FLASH ADDS (dmx-over-wifi.md): swap platform -> hardware_s3.yaml, then
#   substitutions:      absence_timeout: 60s   # dwell room; also bridges LD2450 still-target dropout
#                       dmx_tx_pin: "6"   # D5, radar room
#   packages:           radar: !include ../packages/ld2450.yaml   # NOT ld2410 —
#                         the tracking radar IS the presence radar here
#                         (2026-08-20 consolidation; D6/D7, standard position)
#                       dmx: !include ../packages/dmx_out.yaml
#   external_components: [{source: {type: local, path: ../components}}]
