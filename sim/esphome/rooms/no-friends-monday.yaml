# No Friends Monday sensor node — trigger map from triggers.json
substitutions:
  node_name: lohp-node-no-friends-monday
  room: "No Friends Monday"
  effect: "NoFriendsMonday"
  server_host: "127.0.0.1"   # sim: local server; real node: 192.168.1.238
  api_port: "6069"
  host_mac: "06:35:69:00:00:09"

packages:
  logic: !include ../packages/logic.yaml
  tripwire: !include ../packages/tripwire.yaml
  game: !include ../packages/game_lightsout.yaml
  platform: !include ../packages/sim_host.yaml   # -> hardware_s3.yaml when flashing (S3 = fleet standard)

# FLASH ADDS (dmx-over-wifi.md): swap platform -> hardware_s3.yaml, then
#   substitutions:      dmx_tx_pin: "6"   # D5, radar room
#   packages:           dmx: !include ../packages/dmx_out.yaml
#                       game_hw: !include ../packages/game_lightsout_hw.yaml
#                         # ^ truck lamps (D1) + button ladder (D0 ADC); set
#                         #   truck_chipset/truck_rgb_order to the stash strip
#   external_components: [{source: {type: local, path: ../components}}]
