# Copy to secrets.yaml when building the hardware variant (gitignored).
# Production LAN = LOHP-ESP, the AP on the server Pi behind the RUT140
# (wiring-guides/maze-network.md).
wifi_ssid: "LOHP-ESP"
wifi_password: "change-me"
