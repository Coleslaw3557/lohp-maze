version: '3'
services:
  remote-unit-client:
    build: .
    devices:
      - "/dev/snd:/dev/snd"
    privileged: true
    volumes:
      - ./:/app
      - ./cache:/app/cache
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
